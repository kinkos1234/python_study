# 튜토리얼 2: 기본 데이터 탐색 (Basic Data Exploration)

> 원본: [Kaggle - Basic Data Exploration](https://www.kaggle.com/code/dansbecker/basic-data-exploration)  
> 실습: [Ex 1: 데이터 탐색](../exercises/ex1_explore_your_data.ipynb)

---

## 🎯 학습 목표

- Pandas 라이브러리를 사용하여 데이터를 불러오고 기본 정보를 확인한다
- `describe()` 함수로 데이터의 통계적 요약 정보를 해석한다

---

## 📖 내용 정리

### Pandas로 데이터 탐색하기

머신러닝 프로젝트의 첫 번째 단계는 **데이터에 익숙해지는 것**입니다. 이를 위해 **Pandas** 라이브러리를 사용합니다.

> Pandas는 데이터 탐색과 조작을 위한 데이터 과학자들의 주요 도구입니다.

관례적으로 Pandas는 `pd`로 줄여서 임포트합니다:

```python
import pandas as pd
```

---

### DataFrame

Pandas의 가장 중요한 데이터 구조는 **DataFrame**입니다.

- 엑셀의 시트나 SQL의 테이블과 비슷한 **표 형태** 데이터
- **행(row)** 은 각 관측치(예: 집 한 채)
- **열(column)** 은 각 특성(예: 방 수, 건축 연도 등)

---

### 데이터 불러오기

멜버른(Melbourne) 주택 가격 데이터를 예시로 사용합니다.

```python
# 파일 경로를 변수에 저장 (코드 관리가 쉬워짐)
melbourne_file_path = '../input/melbourne-housing-snapshot/melb_data.csv'

# 데이터를 읽어서 DataFrame으로 저장
melbourne_data = pd.read_csv(melbourne_file_path)

# 데이터 요약 통계 출력
melbourne_data.describe()
```

> 💡 실습에서는 Melbourne 대신 **Iowa(아이오와)** 주택 데이터를 사용합니다.

---

### `describe()` 결과 해석

`describe()`를 실행하면 각 숫자 열에 대해 **8가지 통계값**이 출력됩니다:

| 통계값    | 의미                               |
| --------- | ---------------------------------- |
| **count** | 결측값(NaN)이 아닌 데이터의 수     |
| **mean**  | 평균값                             |
| **std**   | 표준편차 (값이 얼마나 퍼져 있는지) |
| **min**   | 최솟값                             |
| **25%**   | 1사분위수 (하위 25% 지점의 값)     |
| **50%**   | 중앙값 (median)                    |
| **75%**   | 3사분위수 (하위 75% 지점의 값)     |
| **max**   | 최댓값                             |

#### 백분위수(Percentile) 이해하기

데이터를 가장 작은 값부터 큰 값 순서로 정렬한다고 상상하세요:

```
[최솟값] ---[25%]---[50%]---[75%]--- [최댓값]
```

- **25%**: 전체 데이터의 하위 25%가 이 값보다 작습니다
- **50%**: 딱 중간값 (평균과 다를 수 있음!)
- **75%**: 전체 데이터의 하위 75%가 이 값보다 작습니다

---

### 결측값 (Missing Values)

`count`가 다른 통계값보다 작으면, 해당 열에 **결측값**이 있다는 의미입니다.

예: 침실이 1개인 집에서는 "2번째 침실 크기"를 측정하지 않으므로 결측값이 생깁니다.

> ⚠️ 결측값 처리는 이후 강좌에서 다룹니다. 지금은 결측값이 있다는 것만 인지합니다.

---

## 💻 전체 코드

```python
import pandas as pd

# 데이터 로드
melbourne_file_path = '../input/melbourne-housing-snapshot/melb_data.csv'
melbourne_data = pd.read_csv(melbourne_file_path)

# 기본 통계 요약
melbourne_data.describe()
```

---

## 💡 핵심 정리

- `pd.read_csv()`: CSV 파일을 DataFrame으로 불러옴
- `DataFrame`: 행과 열로 구성된 표 형태 데이터 구조
- `describe()`: 수치형 열의 기본 통계를 한 번에 확인
- `count`가 작으면 결측값이 있다는 신호

---

## ➡️ 다음 단계

- 📝 [실습 1: 데이터 탐색](../exercises/ex1_explore_your_data.ipynb) 완료 후
- [튜토리얼 3: 첫 번째 머신러닝 모델](03_first_ml_model.md)로 이동
